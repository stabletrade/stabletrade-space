'use client';

import React from 'react';

const Loading = () => {
  return (
    <div className='fixed inset-0 z-[9999] flex items-center justify-center bg-[#141a36]'>
      Loading...
    </div>
  );
};

export default Loading;
