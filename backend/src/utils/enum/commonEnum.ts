export enum ORDER_TYPE {
  BUY,
  SELL,
}

export enum ORDER_MATCH_TYPE {
  PARTIAL,
  FULL,
}

export enum ORDER_STATUS {
  CANCEL,
  ACTIVE,
}

export enum ORDER_PRICE_TYPE {
  FIXED,
  FLEXIBLE,
}

export enum PRICE_TYPE {
  FIXED,
  FLEXIBLE,
}

export enum REWARD_STATUS {
  PENDING,
  CLAIMED,
}

export enum REWARD_TYPE {
  VOLUME,
  STAKE,
}

export enum STAKE_TYPE {
  STAKE,
  UNSTAKE,
}

export enum STAKE_STATUS {
  PENDING,
  CLAIMED,
}
